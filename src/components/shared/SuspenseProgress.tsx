import { CircularProgress } from '@mui/material'

export default function SuspenseProgress() {
  return (
    <div className='w-full h-full flex items-center justify-center'>
      <CircularProgress />
    </div>
  )
}
